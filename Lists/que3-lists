#print the largest number.

l = [12,23,34,56,74,75]

largest = l[0]
index = 0

for i in range(len(l)):
    if l[i] > largest:
        largest = l[i]
    index = i

print(f"largest number is {largest} at index {index}.")